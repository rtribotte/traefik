# The randomplugin middleware, referenced by the checkout router as
# randomplugin@file. The plugin takes no options and its name reveals nothing,
# so inspecting this middleware tells you nothing about the latency it adds.
http:
  middlewares:
    randomplugin:
      plugin:
        randomplugin: {}
