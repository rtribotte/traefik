# Short-lived demo certificate (generated by scripts/gen-certs.sh).
# Mounted into the file-provider directory so cert_expiry can flag it.
tls:
  certificates:
    - certFile: /certs/demo.crt
      keyFile: /certs/demo.key
